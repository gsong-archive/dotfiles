name: "general"
windows:
  - name: rds
    root: ~/src/btc/btc-fe-web
    layout: even-vertical
    panes:
      - commands:
          - nvm use default
          - npm run docz:start
      - commands:
          - nvm use default
          - npx nx test design-system --watch
      - commands:
          - nvm use default
          - git fetch -p -P
        focus: true
