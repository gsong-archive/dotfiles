name: "general"
windows:
  - name: grid
    root: ~/src/gsong/react-listitem-grid
    layout: even-vertical
    panes:
      - npm start
      - npm t
      - commands:
          - git fetch -p -P
        focus: true
